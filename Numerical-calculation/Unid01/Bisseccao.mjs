class Bisseccao {
  #f;
  #equation;
  #erro;

  constructor(f, equation, erro) {
    this.#f = f;
    this.#equation = equation;
    this.#erro = erro;
  }

  f() {
    return Math.pow(x, 2) || this.#f;
  }

  bisseccao() {}

  calculate() {}
}

const metodoBisseccao = new Bisseccao();
metodoBisseccao.calculate();
